{
  "name": "demosensor",
  "displayName": "demosensor",
  "description": "A demo sensor",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.94.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
    ]
  },

  "theiaPlugin": {
    "headless": "./out/extension.js"
  },
  "headless": {
    "activationEvents": [
      "*"
    ],
    "contributes": {}
  },
  "scripts": {
    "vscode:prepublish": "yarn run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "yarn run compile"
  },
  "devDependencies": {
    "@types/vscode": "1.94.0",
    "typescript": "^5.6.2",
    "headlessdemo": "0.0.0"
  }
}
